#include <bits/stdc++.h>
using namespace std;
#define all(x) begin(x), end(x)
#define X first
#define Y second
#define ll long long
#define fastio ios_base::sync_with_stdio(false);cin.tie(NULL);

int maxHeightOfTriangle(int red, int blue) {
        int mxb=max(red,blue);
        int mnb=min(red,blue);
        int hei=0;
        while(mnb>0){
            
        }   
    }

int main()
{
   fastio

}