#include <bits/stdc++.h>
using namespace std;
#define all(x) begin(x), end(x)
#define X first
#define Y second
#define ll long long
#define fastio ios_base::sync_with_stdio(false);cin.tie(NULL);

int maximumLength(vector<int>& nums) {
        int n=nums.size();
        deque<int>evq;
        deque<int>odq;
        int evsum=0;
        for(int a=0;a<n;a++){
            if(nums[a]%2==0){
                evq.push_back(nums[a]);
                sum+=nums[a];
            }
            else{

            }
        }

        }

int main()
{
   fastio

}