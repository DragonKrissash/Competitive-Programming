#include <bits/stdc++.h>
using namespace std;
#define all(x) begin(x), end(x)
#define X first
#define Y second

void queen(vector<vector<char>>&board,int row,vector<vector<string>>&ans){
    
}


vector<vector<string>> solveNQueens(int n) {
     vector<vector<char>>board(n,vector<char>(n,'.'));
     vector<vector<string>>ans;
     queen(board,0,ans);
    }

int main()
{
ios_base::sync_with_stdio(false);
cin.tie(NULL);
}